must config -- one time -- user.name  and user.email

git config --global user.name "arpit jain"

git config --global user.email "arpit_jain2@persistent.com"

git config  --list


git init //Initialize a git repository

git status //Current status

git status //Current status

git add <Filename> //To add new unchecked file in directory

[comment must in present tense
new change
git commit -m "req123: file1.txt new First file created"

git config --global core.editor "notepad++"
<head> Changes done on local<head>
git add <pattern>
git add .  <for everything>
git add *.py 

git commit -am "some message"

